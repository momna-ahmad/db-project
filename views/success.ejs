<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light grey background */
            color: #212529; /* Dark text */
        }
        .thank-you-container {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .thank-you-card {
            background-color: #ffffff;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn-custom {
            background-color: #212529; /* Black */
            color: #ffffff; /* White text */
        }
        .btn-custom:hover {
            background-color: #343a40; /* Slightly lighter black for hover effect */
            color: #ffffff;
        }
    </style>
</head>
<body>
    <div class="thank-you-container">
        <div class="thank-you-card text-center">
            <h1 class="mb-4">Thank You, <%= name %>!</h1>
            <p class="mb-4">Your order has been placed successfully.</p>
            <div class="d-flex justify-content-center gap-3">
                <a href="/" class="btn btn-custom">Go Back</a>
                <a href="/your-order" class="btn btn-custom">Order Details</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (Optional for dynamic components) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>