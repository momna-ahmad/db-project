<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/form.css">
    <style>
        /* Make the card take full height of the viewport */
        .card {
            width: 100%;
            height: 100%;
            max-width: 28rem; /* Optional: You can set the maximum width for the card */
        }

        /* Ensure the card body stretches to fit the content */
        .card-body {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100%;
        }

        /* Optional: Make sure the body content fits well */
        body {
            height: 100vh;
        }
    </style>
</head>

<body class="container bg-light d-flex align-items-center justify-content-center vh-100">

    <!-- Edit Profile Form -->
    <div class="card shadow-lg">
        <div class="card-body">
            <!-- This will be replaced by your content -->
            <%-body %>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>










